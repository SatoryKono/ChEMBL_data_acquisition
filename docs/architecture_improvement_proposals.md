# Предложения по улучшению архитектуры ETL-пайплайна

1. **Описание:** сформировать слой `library/application/pipelines.py`, предоставляющий программный API для запуска пайплайнов без CLI-оберток, и использовать его в оркестраторе вместо прямых вызовов `main()`.
   **Проблема:** реестр шагов (`PipelineStep.main`) сейчас привязан к сигнатурам CLI (`Callable[[Sequence[str] | None], int]`), поэтому `scripts/get_data.py` вынужден собирать списки аргументов и вызывать их как консольные команды, что смешивает уровни и осложняет повторное использование в тестах и других сервисах.【F:library/pipelines/registry.py†L30-L64】【F:scripts/get_data.py†L1-L120】
   **Ожидаемый эффект:** отделение прикладного уровня от CLI упростит модульное тестирование, позволит повторно использовать шаги в других оркестраторах (например, Power Query-коннекторах) и снизит количество кода, зависящего от строковых аргументов.

2. **Описание:** вынести общую инициализацию среды (manipulation `sys.path`, `ensure_project_root`) в единый пакет `library/bootstrap`, а CLI-скрипты заменить на консольные entry points из `pyproject.toml`.
   **Проблема:** каждый скрипт дублирует десятки строк для настройки окружения, что повышает риск рассинхронизации и усложняет поддержку (см. fallback-блоки в `get_activity_data.py`, `get_assay_data.py`, `get_testitem_data.py`).【F:scripts/get_activity_data.py†L21-L60】【F:scripts/get_assay_data.py†L17-L34】【F:scripts/get_testitem_data.py†L22-L34】
   **Ожидаемый эффект:** централизованный bootstrap устранит дублирование, облегчит публикацию как пакета и обеспечит воспроизводимость запуска в любых окружениях.

3. **Описание:** разделить монолит `library/config.py` на подмодули (`loader`, `validators`, `providers`, `paths`) и внедрить фабрику конфигураций, которая собирает итоговый `Config` из этих компонентов.
   **Проблема:** файл одновременно отвечает за нормализацию путей, подготовку HTTP-сессий, загрузку ресурсов и работу с переменными окружения, из-за чего его трудно тестировать и расширять (более 1k строк вспомогательных функций).【F:library/config.py†L1-L120】
   **Ожидаемый эффект:** декомпозиция повысит читаемость, облегчит добавление новых источников (UniProt, Power Query) и позволит адресно покрывать тестами отдельные части конфигурации.

4. **Описание:** перенести дефолтный реестр пайплайнов (`_DEFAULT_DEFINITIONS`) в версионируемый YAML (`config/pipelines.yaml`) с поддержкой зависимостей между шагами.
   **Проблема:** текущие определения зашиты в код, поэтому любые изменения порядка или зависимостей требуют релиза пакета; также отсутствует механизм описания графа или условного запуска шагов.【F:library/pipelines/registry.py†L76-L119】
   **Ожидаемый эффект:** конфигурационный реестр позволит обновлять пайплайн без правок кода, облегчить эксперименты и обеспечит воспроизводимость сборок через фиксацию YAML в git.

5. **Описание:** внедрить `PipelineContext` (например, `library/application/context.py`), который один раз создает `ChemblClient`, глобальные rate-limiters и вспомогательные словари, а затем прокидывается во все этапы.
   **Проблема:** в каждом скрипте повторяется ручная настройка `get_global_limiter`, создание `ChemblClient` и локальные кэши, что приводит к дублированию и расхождению поведения при обновлениях конфигурации.【F:scripts/get_activity_data.py†L999-L1079】【F:scripts/get_assay_data.py†L289-L336】
   **Ожидаемый эффект:** единый контекст обеспечит согласованную конфигурацию клиентов, сократит время прогрева (повторное использование сессий) и облегчит мокирование в тестах.

6. **Описание:** вынести формирование отчётов качества таблиц и их вызов в `library/qa/reporting.py`, оставив в пайплайнах только декларативные хуки.
   **Проблема:** логика генерации отчётов (partial на `analyze_table_quality`, проверка флагов) дублируется в нескольких скриптах, что усложняет контроль параметров и ведёт к расхождениям в отчётности.【F:scripts/get_activity_data.py†L999-L1012】【F:scripts/get_assay_data.py†L289-L301】【F:scripts/get_document_data.py†L691-L705】
   **Ожидаемый эффект:** централизованный QA-слой обеспечит единые параметры выборки, повысит воспроизводимость метрик и облегчит интеграцию с CI-отчётами.

7. **Описание:** консолидировать пакет `library/testitem_pipeline` с модульной архитектурой остальных пайплайнов (`library/pipelines/testitem`) и предоставить единые точки расширения для обогащения данными словарей.
   **Проблема:** скрипт `get_testitem_data.py` реэкспортирует десятки внутренних функций и напрямую зависит от приватных модулей `library/testitem_pipeline/cli.py`, что нарушает единообразие слоёв и затрудняет переиспользование логики в других пайплайнах.【F:scripts/get_testitem_data.py†L36-L85】【F:library/testitem_pipeline/cli.py†L1-L120】
   **Ожидаемый эффект:** унификация пакетов снимет необходимость в ручных реэкспортах, облегчит настройку тестов и позволит использовать общие утилиты (chunked fetch, metadata hooks).

8. **Описание:** разбить доменные преобразования (нормализация, пост-обработка, QA) на явные слои `integration → domain_service → presentation`, чтобы CLI импортировал только сервисный слой.
   **Проблема:** CLI `get_activity_data.py` напрямую импортирует низкоуровневые интеграции, постобработку и схему одновременно, что затрудняет навигацию и контроль зависимостей между слоями.【F:scripts/get_activity_data.py†L64-L108】
   **Ожидаемый эффект:** введение доменного сервиса сделает слои явными, улучшит читаемость и позволит изолировать Power Query/M трансформации или альтернативные источники без изменения CLI.

9. **Описание:** заменить глобально мутируемые объекты схем (например, `ActivitiesSchema` с обновлением `columns[...]`) на фабрики, возвращающие неизменяемые экземпляры и регистрирующиеся в общем реестре.
   **Проблема:** функция `configure_activity_schema` очищает и модифицирует глобальный список проверок, что создаёт скрытое состояние и может приводить к гонкам при параллельных запусках или тестах.【F:library/schemas/activities.py†L52-L121】
   **Ожидаемый эффект:** неизменяемые схемы обеспечат детерминизм валидации, облегчат кеширование и позволят явно фиксировать версии схем для отчётности.

10. **Описание:** создать модуль `library/reporting/run_manifest.py`, который собирает статистику (`rows_total`, `rows_kept`, хэши) и формирует meta/quality файлы для всех пайплайнов на основе стандартизированных структур.
    **Проблема:** генерация YAML-метаданных и quality-репортов реализована вручную в `get_document_data.py`, что ведёт к расхождениям формата и необходимости поддерживать логику в каждом скрипте отдельно.【F:scripts/get_document_data.py†L664-L705】
    **Ожидаемый эффект:** единый механизм отчётности обеспечит согласованные артефакты, упростит интеграцию с CI/CD и снизит риск ошибок при модификации форматов.
