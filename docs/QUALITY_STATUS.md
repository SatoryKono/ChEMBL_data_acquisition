# Статический отчёт о качестве

_Снимок состояния на 2025-10-09._

## Тестовый контур

| Метрика | Значение |
|---------|----------|
| Команда | `python scripts/run_tests.py` |
| Продолжительность | 8.28 с |
| Success rate | **0.00%** |
| Всего тестов | 1 |
| Ошибок | 1 |

### Обязательные команды CI

В непрерывной интеграции всегда запускаются следующие команды (в указанном
порядке). Изменения в пайплайне должны поддерживать их успешное выполнение и
детерминированные артефакты отчётности:

1. `python scripts/run_tests.py` — pytest + генерация `reports/test_report.json`
   и `reports/test_summary.md` с проверкой порога **95 %** успешных тестов.
2. `ruff check` — статический анализ кода, ошибок форматирования и импорта.
3. `mypy --strict library` — строгая проверка типизации основной кодовой базы.

### Детали сбоя
- `tests/unit/test_check_determinism.py` не импортируется: `scripts/check_determinism.py` завершается `SyntaxError` (незакрытая скобка). Это блокирует выполнение всей сессии pytest.

## Линтер (ruff)

| Параметр | Значение |
|----------|----------|
| Команда | `ruff check` |
| Диагностики | 13 296 |
| Преобладающие коды | `I001` (несортированные импорты), `UP035` (устаревшие импорты) |

### Примечания
- Линтер останавливается только после вывода всех диагностик; необходимо упорядочить импорты и перенести типы из `typing` в `collections.abc`.

## Типизация (mypy)

| Параметр | Значение |
|----------|----------|
| Команда | `mypy --strict library` |
| Ошибок | 380 |
| Ключевые категории | Несовместимые присваивания (`assignment`), аргументы неправильного типа (`arg-type`), избыточные/неиспользуемые `type: ignore` |

### Примечания
- Наиболее проблемные участки: `library/config/models.py`, `library/clients/chembl.py`, `library/table_quality.py`, `library/processing/activity/annotations.py`.
- Для разблокировки strict-режима требуется пересмотреть модели конфигураций (опциональные поля), типизацию HTTP-клиентов и обработку дат/таймстемпов в quality-аналитике.

## Как обновить отчёт

1. Активировать окружение: `source .venv/bin/activate`.
2. Сгенерировать отчёты:
   ```bash
   python scripts/run_tests.py
   ruff check > ruff-report.txt
   mypy --strict library > mypy-report.txt
   ```
3. Перенести обновлённые значения в эту страницу.
