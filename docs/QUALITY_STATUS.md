# Статический отчёт о качестве

_Обновлено: 2025-10-13 (UTC). Свежий QA-отчёт: [`reports/test_summary.md`](../reports/test_summary.md)._【F:reports/test_summary.md†L1-L18】

## Тестовый контур

| Метрика | Значение |
|---------|----------|
| Команда | `python scripts/run_tests.py` |
| Статус | ❌ Юнит-тест `test_save_standard_outputs__uses_canonical_naming_and_cleans_source` не переименовывает legacy-файл |
| Продолжительность | 30.67 s |
| Success rate | 75.00% |
| Всего тестов | 4 |
| Ошибок | 0 |

### Обязательные команды CI

В непрерывной интеграции всегда запускаются следующие команды (в указанном
порядке). Изменения в пайплайне должны поддерживать их успешное выполнение и
детерминированные артефакты отчётности:

1. `python scripts/run_tests.py` — pytest + генерация `reports/test_report.json`
   и `reports/test_summary.md` с проверкой порога **95 %** успешных тестов.
2. `pytest -q` — минимальный smoke-прогон без отключения предупреждений.
3. `ruff check` — статический анализ кода, ошибок форматирования и импорта.
4. `mypy --strict library` — строгая проверка типизации основной кодовой базы.
5. `pre-commit run --all-files` — консистентная обвязка локальных и CI-проверок.

### Детали сбоя
- `python scripts/run_tests.py` останавливается на `tests/unit/io/test_output_writer.py::test_save_standard_outputs__uses_canonical_naming_and_cleans_source`: `save_standard_outputs` оставляет артефакт `result.tmp.csv` и не переименовывает его в каноническое `output.testitem_20240101.csv`, из-за чего success rate фиксируется на уровне 75 % и политика завершает прогон.【2305f4†L1-L84】【8a35f5†L13-L41】
- `pytest -q` валится на e2e-сценарии `test_activity_logging__relative_env_base_anchored_to_repo_root`: CLI делает HTTP-запрос, который блокируется фикстурой `deny`, поэтому тест завершается `AssertionError` и success rate падает до 0 %.【80f00a†L1-L33】

## Линтер (ruff)

| Параметр | Значение |
|----------|----------|
| Команда | `ruff check` |
| Статус | ❌ |
| Диагностики | 24 |
| Преобладающие коды | `UP038`, `E402`, `B010`, `F401`, `F821`, `UP037` |

### Примечания
- `ruff` ловит 24 нарушения (19 автоматически исправимы): устаревшие `noqa`, запрещённые `setattr`, неиспользуемые импорты и перенос на `Path` после ленивого импорта в скриптах CLI.【8a8027†L1-L83】

## Типизация (mypy)

| Параметр | Значение |
|----------|----------|
| Команда | `mypy --strict library` |
| Статус | ❌ |
| Ошибок | >400 |
| Ключевые категории | Отсутствующие параметры обобщений Pandas (`Series`, `Index`), несогласованные типы `Path`/`str`, несоответствие TypedDict и конфликтные аннотации CLI |

### Примечания
- Установленный dev-стек запускает строгий mypy, но проверка фиксирует сотни ошибок уже на ранних модулях (`library/resources/dictionaries.py`, `library/common/text_utils.py`, `library/config/models.py` и др.), поэтому отчёт требует масштабной чистки типов и стубов Pandas.【900e7b†L19-L87】

## Pre-commit

| Параметр | Значение |
|----------|----------|
| Команда | `pre-commit run --all-files` |
| Статус | ❌ |
| Причина | Хуки `ruff`, `black`, `mypy` и `pytest` завершаются ошибками (некорректные стили, сотни типовых проблем и сетевые обращения в e2e) |

### Примечания
- `ruff` и `black` пытаются автоисправить десятки файлов, `mypy` повторяет полный список типовых ошибок, а финальный хук `pytest` падает на сетевом вызове `test_activity_logging__relative_env_base_anchored_to_repo_root`, поэтому весь пайплайн pre-commit остаётся красным.【be9214†L1-L18】【900e7b†L1-L87】【9e0e16†L1-L32】

## Регламент актуализации

1. После каждого релиза и при значимых изменениях пайплайна запускать полный набор обязательных команд CI, фиксируя вывод в QA-отчёте (`reports/test_report.json` и краткая сводка в `reports/test_summary.md`).
2. Проверять наличие актуальных отчётов в каталоге `reports/tests/` и добавлять ссылку на свежий документ в раздел «Статический отчёт о качестве».
3. Обновлять таблицы статусов и раздел «Риски» на основании последних прогонов; если проблема устранена, переносить запись в архив либо удалять из текущего файла.
4. При установке зависимостей следить за совместимостью версий с текущим Python: `types-pytest` и связанные пакеты требуют выпуски, поддерживающие Python 3.11. С 2025-10-11 зависимость ограничена маркером `python_version < "3.11"`, поэтому `pip install -r requirements-dev.txt` на Python 3.11.12 завершился успешно; при обновлении стубов необходимо вернуть совместимую версию и задокументировать результат.【145da3†L1-L12】【6599c7†L1-L12】
5. Поддерживать ссылку на используемое окружение (venv/conda) и контрольный список действий в этом разделе; перед публикацией PR сверять документ с фактическими логами CI.

## Как обновить отчёт

1. Активировать окружение: `source .venv/bin/activate`.
2. Сгенерировать отчёты:
   ```bash
   python scripts/run_tests.py
   ruff check > ruff-report.txt
   mypy --strict library > mypy-report.txt
   ```
3. Перенести обновлённые значения в эту страницу.

## Риски

- **Регрессия в `output_writer.save_standard_outputs`.** Функция не переименовывает временные CSV-файлы и не очищает метаданные, поэтому падает ключевой юнит-тест и итоговая success rate тестового контура ниже пороговых 95 %.【2305f4†L1-L84】【8a35f5†L13-L41】
- **E2E-сценарии не изолированы от сети.** Запуск `pytest -q` инициирует HTTP-запросы в `test_activity_logging__relative_env_base_anchored_to_repo_root`, которые блокируются тестовой фикстурой и делают весь smoke-прогон красным.【80f00a†L1-L33】
- **Статический анализ и хуки pre-commit фиксируют большой технический долг.** `ruff`, `black`, `mypy` и `pytest` внутри pre-commit стопорят цепочку из-за стиля, аннотаций и сетевых побочных эффектов; без наведения порядка локальные проверки и CI остаются нестабильными.【be9214†L1-L18】【900e7b†L1-L87】【9e0e16†L1-L32】

## Статус CI

- GitHub Actions (основной пайплайн): https://github.com/SatoryKono/ChEMBL_data_acquisition/actions — текущая конфигурация должна запускать `pytest`, `ruff`, `mypy` и `pre-commit` после восстановления зависимостей.
