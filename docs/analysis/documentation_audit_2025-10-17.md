# Documentation audit — 2025-10-17

| № | Документ | Раздел / Файл | Найденная проблема | Исправление / Обновление |
|---|-----------|---------------|---------------------|---------------------------|
| 1 | README_EN.md | Quick start | Инструкция использовала ручную установку `pip install .[dev]`, расходясь с каноническим README и фактическим сценарием `make init`. | Скопирована актуальная англоязычная версия README с описанием `make init`, проверки `.python-version` и политикой `pre-commit`. |
| 2 | README_RU.md | § «Быстрый старт», блок про pre-commit | Отсутствовали шаги `make init`, проверка `.python-version` и описание `pre-commit install --install-hooks`, что приводило к устаревшим инструкциям для разработчиков. | Обновлён раздел: добавлены команды `make init`, ручной эквивалент, примечания по синхронизации зависимостей и полный перевод раздела про `pre-commit`. |
| 3 | Экспорт протокола (`docs/en/PROTOCOL_EN.md`, `scripts/convert_md_to_docx.py`) | Протокол | В репозитории не было актуального DOCX, только Markdown-версия `docs/en/PROTOCOL_EN.md`, что нарушало требование CHEMBL-DM01. | Добавлен воспроизводимый сценарий экспорта через `scripts/convert_md_to_docx.py`; бинарный DOCX исключён из Git, чтобы собирать его по требованию (см. CI-артефакты). |
| 4 | docs/en/CHANGELOG.md, docs/ru/CHANGELOG.md | Release log | Журнал изменений не фиксировал октябрьскую синхронизацию документации и выпуск DOCX. | Добавлена запись `0.1.4` с описанием аудита и обновления артефактов в обеих языковых версиях. |
