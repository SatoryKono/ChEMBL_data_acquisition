# Discrepancy Table

| № | Document | Section / File | Content in documentation | Realization in code | Type of discrepancy | Recommendation |
|---|---|---|---|---|---|---|
| 1 | Architectural Overview | `library/` | The presence of a `library/postprocess` directory is implied in some older documentation. | The directory `library/postprocess` does not exist; logic is in `library/postprocessing`. | Obsolete information | Update all references to point to `library/postprocessing`. |
| 2 | ChEMBL Data Acquisition Protocol | `docs/` vs `scripts/` | Multiple versions of the protocol exist in different formats (`.md` and `.docx`) and locations. | The `docs` directory contains `ChEMBL Data Acquisition Protocol v2.2.md`, while `scripts` has older `.docx` versions (v0.2.1, v0.3.1, v0.3.2). | Version mismatch | Consolidate to a single, up-to-date version in the `docs` directory and remove the old `.docx` files. |
| 3 | Configuration Guide | `config/pipeline/` | The dual-file YAML configuration system (e.g., `activity.yaml` for high-level orchestration and `activities.yaml` for post-processing) is not clearly explained. | The codebase uses this dual-file system, but the documentation lacks a clear explanation of the separation of concerns. | Incomplete documentation | Add a section to the `CONFIG.md` guide explaining the purpose of the two types of YAML files for each pipeline. |
| 5 | Architectural Diagrams | `docs/en/ARCHITECTURE.md` | The diagrams show a `library.testitem_pipeline` module with a note about a legacy shim. | The logic has been fully migrated to `library.pipelines.testitem`, and the shim might be a candidate for removal. | Architectural drift | Verify if the shim is still necessary. If not, update the diagram and remove the shim. |
| 6 | Project Memory | `scripts/` | `get_target_data.py` uses subcommands, while `get_document_data.py` uses a `--mode` flag. | This is an inconsistency in the CLI design. | Inconsistent design | Document this inconsistency in the developer guide and consider unifying the CLI design in a future refactoring. |
| 7 | Project Memory | `library/` | Redundant and undocumented subdirectories like `postprocessing/` alongside `postprocess/` are mentioned. | The `postprocess` directory does not exist, but the redundancy concern is valid as the naming could be clearer. | Architectural inconsistency | Clarify the naming convention in the documentation and, if possible, rename the `postprocessing` directory to `postprocess` for consistency. |
| 8 | Project Memory | `pyproject.toml` | The `pyproject.toml` might have a strict Python version requirement. | The `pyproject.toml` should be checked for a strict version requirement (e.g., `==3.11.*`) that could be relaxed to `>=3.11`. | Environment constraint | Update the `pyproject.toml` to allow for a broader range of Python versions. |
| 9 | Project Memory | `docs/` | The user requires Russian and English documentation files to be named with `_RU.md` and `_EN.md` suffixes, respectively. | Many documentation files in the root `docs` directory do not follow this convention (e.g., `USAGE_EN.md`). | Naming inconsistency | Rename the documentation files in the `docs` directory to follow the `_EN.md` and `_RU.md` suffix convention. |
