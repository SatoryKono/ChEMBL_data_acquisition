[tox]
envlist = clean
requires = tox>=4
skipsdist = true

[testenv:clean]
description = Remove build artefacts, cached reports and temporary pipeline outputs.
skip_install = true
allowlist_externals = python
commands =
    python scripts/cleanup_project.py --all
